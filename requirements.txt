# SOLUCIÓN DEFINITIVA PARA RENDER.COM
# Python 3.13 + pydantic compatible

# 1. PRIMERO instalar pydantic-core compatible
pydantic-core==2.25.0

# 2. LUEGO pydantic que use esa versión
pydantic==2.7.1

# 3. FastAPI stack compatible
fastapi==0.110.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
typing-extensions==4.12.0

# 4. Data science (Python 3.13 compatible)
numpy==2.1.0
pandas==2.2.3

# 5. Utilities
requests==2.31.0
python-dotenv==1.0.0